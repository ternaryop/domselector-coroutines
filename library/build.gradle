apply plugin: 'com.android.library'
apply plugin: 'kotlin-android'
apply plugin: 'kotlin-android-extensions'
apply from: '../mavenlocal.gradle'
apply from: '../bintrayconfig.gradle'

android {
    compileSdkVersion rootProject.ext.compileSdkVersion
    buildToolsVersion rootProject.ext.buildToolsVersion

    compileOptions {
        sourceCompatibility 1.8
        targetCompatibility 1.8

        // fix the error "More than one file was found with OS independent path 'META-INF/library_release.kotlin_module'"
        // when used inside the app
        kotlinOptions.freeCompilerArgs += ['-module-name', "${rootProject.ext.publishPackage.groupId}.${rootProject.ext.publishPackage.artifactId}"]
    }

    defaultConfig {
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        versionCode 1
        versionName rootProject.ext.publishPackage.version

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"

    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    api "com.google.code.gson:gson:${rootProject.ext.gsonLibVersion}"

    implementation "org.jetbrains.kotlinx:kotlinx-coroutines-core:${rootProject.ext.kotlinxCoroutinesCore}"

    api "com.squareup.okhttp3:okhttp:${rootProject.ext.okhttp3HttpLibVersion}"

    api "com.squareup.retrofit2:retrofit:${rootProject.ext.retrofitLibVersion}"
    api "com.squareup.retrofit2:converter-gson:${rootProject.ext.retrofitConverterGsonLibVersion}"

    api "org.jsoup:jsoup:${rootProject.ext.jsoupLibVersion}"

    api "com.ternaryop.photoshelf:api-coroutines:${rootProject.ext.ternaryOpPhotoShelfApiCoroutines}"
    api "com.ternaryop:utils:${rootProject.ext.ternaryOpUtilLibVersion}"

    testImplementation "junit:junit:${junitLibVersion}"
    androidTestImplementation "androidx.test:runner:${androidXTestRunner}"
    androidTestImplementation "androidx.test.espresso:espresso-core:${androidXEspressoCoreLibVersion}"

    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk8:$kotlin_version"
}
repositories {
    mavenCentral()
}
